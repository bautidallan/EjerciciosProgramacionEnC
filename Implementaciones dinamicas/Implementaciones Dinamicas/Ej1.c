/*En el programa principal crear un entero dinámicamente mediante el uso de la función malloc, asignarle
el valor 5 y mostrarlo por pantalla. Acto seguido, autoincrementarlo y volver a mostrarlo. Por último,
asignarle un valor ingresado desde teclado y mostrarlo una vez más. */
/*
}
#include "string.h"
#include <stdlib.h>

int main(){
    int *valor;
    valor=(int*)malloc(sizeof(int));
    *valor=5;
    printf("%d \n",*valor);
    (*valor)++;
    printf("%d \n",*valor);
    scanf("%d",valor);
    printf("%d \n",*valor);
    return 0;
}
*/
